# Copy this file to `.env` for local development.
# Do NOT commit your real `.env` to git (it contains secrets).

DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5000/auth_db
JWT_SECRET=your-jwt-secret
CORS_ORIGIN=http://localhost:3001
PORT=5001

# Optional: enable email sending for password reset
EMAIL_USER=
EMAIL_PASS=

